# 在宝塔终端中执行以下命令：

# 1. 进入正确的目录（使用绝对路径）
cd /www/wwwroot/xl-im

# 2. 查看目录内容，确认文件是否存在
ls -la

# 3. 如果脚本没有执行权限，先添加权限
chmod +x *.sh

# 4. 运行启动脚本
./start.sh

# 或者一行命令搞定：
cd /www/wwwroot/xl-im && ls -la && chmod +x *.sh && ./start.sh

